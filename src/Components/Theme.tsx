import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#000080", // Your custom primary color
    },
    secondary: {
      main: "#f0ad4e", // Your custom secondary color
    },
  },
});

export default Theme;
